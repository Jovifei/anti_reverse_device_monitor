# 二期数据质量报告

生成时间：2026-07-22。

## 已验证的本地链路

- SQLite E2E fixture：1 个 CT、1 个微逆、22 条遥测；无孤立遥测、无缺失 SIID/PIID、无过期数据。
- Mock Source→SQLite 集成：同一上报时间的两条不同源记录按 `(reportedAt, sourceRecordId)` 稳定分页；首次同步写入 2 条，第二次同步跳过 2 条重复记录，并只在成功后保存 checkpoint。
- 当前示例指标字典不足，E2E fixture 报告 22 个未知指标；这不是把未知值填零，待真实 SIID/PIID 映射后补全。

## 真实源检查状态

- 最近同步成功、源端最新时间、本地与源端延迟、重复率、时间倒退、未来时间、无法识别微逆序号、长期无状态已配对微逆、在线状态过期、发电量单位和累计回退：等待批准的只读连接与 `config/source-field-mapping.local.json`。
- 本轮未读取真实源库，未收集客户原始数据或凭据。

## 数据质量结论

离线的同步、去重、审计和缺失值保护已经通过；真实源数据质量尚不能结论。
