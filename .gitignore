node_modules/
.next/
coverage/
!.env.example
!.env.local.example
.DS_Store
.vscode/
.vscode
data/
.codex/
.agents/
*.tsbuildinfo
npm-debug.log*

# Local delivery material not needed for production
codex/
prototype/
references/
tasks/
00_START_HERE.md
DELIVERY_MANIFEST.json

# Generated offline HTML snapshots and UI review screenshots.
# Regenerate with: npm run export:html:demo / npm run ui:capture
artifacts/

# Claude Code local config
.claude/

# Environment files (keep examples only)
.env
.env.local
.env.docker
config.txt
config/source-field-mapping.local.json
config/devices.draft.json
config/devices.local.json
config/mongo-field-mapping.local.json
backups/
test-results/
playwright-report/
.next-validation/
.next-ui-capture-*/
.tsconfig-ui-capture-*.json
docs/dependency-audit.raw.json

# Added by code-review-graph
.code-review-graph/

# CodeGraph
.codegraph/

# Comet local runtime state (including per-change .comet/ runtime dirs).
# Tracked spec material under openspec/changes/ stays: proposal.md, design.md,
# tasks.md, specs/, .openspec.yaml, .comet.yaml
.comet/

# AI IDE configs (Cursor, Windsurf, Kiro, Qoder, OpenCode, Gemini)
.cursor/
.cursorrules
.windsurfrules
.kiro/
.qoder/
QODER.md
.opencode.json
.mcp.json
.gemini/

# Local real-log UI exploration; never commit customer telemetry or derived previews.
log/
!docs/data/
ui-preview.html
ui-preview-real-logs.html
scripts/generate-log-preview.cjs
