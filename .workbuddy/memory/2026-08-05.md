# 2026-08-05 工作日志

## Docker 部署诊断（续，anti_reverse_device_monitor_delivery_v1）
- 重新探测网络：宿主机选择性封锁——可直连 baidu/github(200)，但 Docker Hub 与全部国内镜像源(163/百度/道客云/docker-cn)均不可达；本地无缓存 node 基础镜像。
- 无在运行代理：127.0.0.1:7890(Clash)无 LISTENING；:7891 实为 com.docker.backend.exe(Docker 自身端口，非代理)；QQLive.exe 无关；SOCKS5 走 :7891 超时。
- 确认 daemon.json `proxies` 不配置守护进程拉镜像代理（Docker Desktop 拉取仍报 no HTTPS proxy）；拉取代理由 Docker Desktop Settings→Resources→Proxies 控制。
- CLI 无法重启 Docker：wsl 被安全策略禁用(net stop/start 需管理员)。重启须用户在 Docker Desktop 手动做。
- 修复需用户：启动 Clash(7890, Allow LAN) + Docker Desktop→Resources→Proxies 填 http://host.docker.internal:7890 + Apply&Restart。之后我执行 build/up/seed/验证 3102/devices。
- 更新报告：DOCKER_DEPLOY_BLOCKER.md。本地修复文件均未 git 提交。

## DOCKER_DEMO_DEPLOYMENT_VALIDATION（成功，DOCKER_DEMO_STATUS=PASS）
- 用户确认网络已通（7890 LISTENING、pull 成功、镜像缓存）。本轮未改 daemon/json/代理/重启 Docker。
- 分支 `codex/docker-demo-deployment`（基于 origin/main 2385d07，保留工作区）；本地提交 `444d4e4`（18 文件，未推送/合并）。
- 代码修复审计全过：npm ci / prisma:generate(v5.22.0) / typecheck(0) / lint(0) / test(120 unit + integration pass)。
- Migration Gate 过：链 0001-0005 连续；空库 deploy、中间态升级、幂等复验均 RC=0。0001-0003 原为未跟踪，已纳入提交（canonical）。
- **修复真实容器缺陷**：`scripts/docker-entrypoint.sh` 原 CRLF 提交，Linux 容器内 `set -eu\r` 触发 `Illegal option -` 致 app 无限重启；转 LF 并加 `.gitattributes`(*.sh eol=lf) 防回归。
- 新建 `docker-compose.demo.yml`：项目 anti-reverse-demo，SOURCE_DB_ENABLED=false，不读 .env.docker，独立 demo-data 卷，demo-seed 一次性(自带 migrate deploy)+app(healthcheck /api/live)。
- 生产 compose 加固：端口变量/ init/ stop_grace_period/ healthcheck/ 日志轮转/ 去除 SOURCE_*/MONGODB_* 与 env_file 重复。.dockerignore 补 log、ui-preview-real-logs.html。
- 构建 RC=0（镜像 anti-reverse-demo:local ≈371MiB）。demo-seed 退出 0（3 台 DEMO CT + 140272 遥测）。app healthy，/api/live 与 /devices 及 3 台详情均 HTTP 200；无真实 undefined/NaN（仅有 Next RSC `$undefined` 占位符）。
- 持久化验证：restart、up -d --build 后 telemetry=140272/device=3/reverseFlowAlert=3 一致；卷未删（禁止 down -v）。
- Mongo 未访问（Demo 不设 URI/不启 sync）；正式部署前置：Mongo 只读授权(error 13)、真实字段映射、二期联调、启用 sync profile。
- 文档：DOCKER_PREDEPLOY_AUDIT.md / DOCKER_DEMO_DEPLOYMENT_REPORT.md / DOCKER_OPERATIONS.md。
