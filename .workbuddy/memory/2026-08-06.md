# 2026-08-06

## IoT 平台设备注册表自动同步（替掉 Excel 手动层）
- 新增 `src/adapters/iot-api/{types.ts,iot-client.ts}`：造梦者 IoT 平台 HTTP 客户端，POST `/api/device/getDevices` 分页拉全 + GET `/api/device/getDevice` 详情；axios + Zod + 重试（5xx/网络 3 次指数退避，业务 code≠0 不重试）。
- 新增 `scripts/sync-iot-device-registry.ts`：`npm run devices:sync-iot`，支持 `--dry-run`/`--output`/`--product-id`/`--size`/`--prune`。
- `src/adapters/source-db/device-registry.ts`：`deviceEntrySchema` 增 `nickname?`/`online?`，新增 `mergeIotListIntoRegistry`（保留 Excel SN、更新 nickname/online、默认不删旧项，prune 仅删 IoT 不在且无 label 项）。
- Token 走 `DREAM_MAKER_IOT_TOKEN`（`.env.local`），不硬编码、不入 Git（已加固 .gitignore）。
- 默认 productId `689adc659f04ec32f7642fbb`（防逆流 CT 品类）；size 默认 100。
- QA 独立验证 PASS：19/19 单测（交付方 14 + QA 自补 5 边界）。tsc 无新增错误。
- 已知非阻断项：4xx 未包成 IotApiError；online 以 IoT 为准（false 会覆盖旧 true）；createClient 每次新建实例。

## 用户确认
- 排序功能（上一轮的 SN/发电量排序按钮）明确不要了，已砍。

## 提交 GitHub
- 已 commit `7e809b3` 并 push 到 `origin/main`（`4d4ca7f..7e809b3`）。11 文件 / +919 行。
- 暂存时精准排除：`.workbuddy/`、构建产物 `_tsc_*.txt`/`_vitest_out.txt`、`config.txt`(已 gitignore)。无 token 泄漏。
- 下一步用户需把真实 Bearer Token 填入 `config/.env.local`（建议先吊销聊天里暴露的那串再换），再跑 `npm run devices:sync-iot -- --dry-run` 验证。

## 每日 0:00 同步 cmd 自动化（用户主诉求：之前"坏了"）
- 根因：之前根本没有每日同步脚本；`start-monitor.cmd/.ps1` 是一键全栈启动器，从不调 `devices:sync-iot`。
- 新建 `sync-iot-daily.cmd` → `scripts/sync-iot-daily.ps1`：无头跑 `npm run devices:sync-iot` → `config/devices.json`，带 UTF-8 日志 `logs/sync-iot-daily-YYYY-MM-DD.log`。
- 改 `start-monitor.ps1`：顶部 `$PWD` 兜底 `$PSScriptRoot`；新增 `[2b]` IoT 同步步（非致命）。
- 改 `docs/11_OPS_RUNBOOK.md` §1.1：Windows 计划任务 `schtasks /Create /TN "AntiReverse_IoT_DailySync" /TR "...\sync-iot-daily.cmd" /SC DAILY /ST 00:00 /RL HIGHEST`；Docker 走 `POST /api/cron/sync-iot`。
- 已落盘验证：`npm run devices:sync-iot -- --dry-run` 真实接口 372 台 OK；ps1 端到端 OK。脚本只吃 `DREAM_MAKER_IOT_TOKEN`（已存在），不依赖 Mongo 变量。
- ⚠️ 沙箱坑：PowerShell 工具宿主对含中文的 UTF-8 BOM .ps1 解析异常（`$PSScriptRoot`/`$PWD` 变空、行号错乱）；生产 `powershell -File` 正常。已用 `#` 行注释替代 `<#`块注释 + `$PWD` 兜底修复。文件删不动（SAFE_DELETE_FAIL_CLOSED），`logs/` 与 `_*.txt` 临时文件残留未清。

## Comet 改动 iot-registry-daily-sync 收尾（分支 feature/20260807/iot-registry-daily-sync）
- `implementer` 子代理超 40 轮挂掉（非逻辑错），另起 `iot-finisher` 接管。
- 已 commit `8a4288a`：7 文件 / +516 行。`app/devices/page.tsx`（7日以上离线 KPI 卡 + 筛选 + 行样式）、`app/api/cron/sync-iot/route.ts`（POST Bearer `CRON_SECRET` timingSafeEqual、缺失→503、execFile node+tsx 跑同步 120s、GET→{ok:true}）、`tests/unit/cron-sync-iot-route.test.ts`（8 用例）。service 层（classifyStatus/staleOfflineCount/registryTotal）由 implementer 完成已并入。
- 独立验证（lead）：`route.ts`/`test.ts` **0 新类型错误**；`page.tsx` 8 个 TS2322 全是预存 typedRoutes Link href 限制（7 旧卡 + 新增第 8 卡同模式），非本次引入；vitest 20/20。
- 偏差/留待：① GET 按用户指令→{ok:true} 而非设计文档 405；② `.stale-offline-row` 无专属 CSS（回退基础卡，可渲染）；③ 任务 #6（CRON_SECRET 进 .env.local.example / README / package.json cron 脚本 / globals.css）未做，留单独提交。
- cmd 修复 4 文件 + `config/devices.json`（dry-run 改动）**未提交**，等用户拍板。

## 待用户决策
1. 提交 cmd 修复（新 cmd + start-monitor.ps1 + docs）？不自动 commit。
2. 注册 Windows 计划任务（需管理员）？
3. Comet verify→archive + push（push 需明确授权）？
4. 现在补 task #6 样式/config？
5. `.env.local` 缺 `APP_DATABASE_URL`/`MONGODB_URI`/`SOURCE_DB_ENABLED` 等，核对补全？
