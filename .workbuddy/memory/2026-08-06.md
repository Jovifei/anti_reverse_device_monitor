# 2026-08-06 Docker Demo 部署落地 + 推远端

## 本轮完成
- 分支 `codex/docker-demo-deployment`（HEAD `444d4e4`，基于 origin/main `2385d07`）已 `git push -u origin` 成功。
  - 远端已建分支：`refs/heads/codex/docker-demo-deployment`。GitHub 提供开 PR 链接（未合并 main）。
- 清理过时文档 `DOCKER_DEPLOY_BLOCKER.md`（早期网络卡顿诊断，现已 PASS，留着误导）。工作树仅剩未跟踪 `.workbuddy/`（本地记忆，不入库）。
- 确认 Demo 部署在 Docker 实际运行：
  - 容器 `anti-reverse-demo-app-1`：`Up (healthy)`，`127.0.0.1:3102->3000/tcp`
  - `demo-seed` 退出码 0；`/api/live` 200（`status:null`，无 Mongo）；`/devices` 200，三台 Demo CT 渲染
  - 容器内实时数据：device=3 / telemetry=140272 / reverseFlowAlert=3（卷 `anti-reverse-demo_demo-data` 持久化）
- 用户明确要求“推进项目落地放到 Docker 部署上执行” —— 即 Demo 已是实际运行的 Docker 部署系统。

## 下一步前置（生产级部署，未做）
- Mongo 只读授权仍阻塞：`log` 库 find error 13，真实集合 `zeico_cloud.device_log_689adc659f04ec32f7642fbb`。
- 生产 `docker-compose.yml` 已加固（端口变量 3102、init、healthcheck、日志轮转、去除 env 重复），但运行需 `.env.docker` + Mongo；当前 Mongo 未授权 → sync 不工作，app 本身可起但 SQLite 空（无其他 seed 路径）。
- 真实字段映射核对 + 二期真实联调（同步/多设备/压测）待办。

## 关键约定（延续）
- 禁止 `docker compose down -v`（会删 Demo 数据卷）。
- `.gitattributes` 已锁 `*.sh eol=lf`，防 docker-entrypoint.sh 的 CRLF 容器崩溃复发。
- 独立 Demo 部署：`docker compose -p anti-reverse-demo -f docker-compose.demo.yml up -d`。

## 23:xx 闪退恢复 — 用合并代码 23addb0 重建部署
- 会话闪退前已把 origin/main 合并进 `codex/docker-demo-deployment`（HEAD `23addb0`，含造梦者 IoT 同步 `devices:sync-iot` + 12-CT 注册表 `config/devices.json` + `start-monitor.ps1`），但 `up -d --build` 在合并前被打断，运行的仍是旧镜像 `8e460fe`（Aug 6 00:04，合并前）。
- 卡死根因：Docker daemon 无默认外网出口，旧构建直连外网挂死（17 分钟零输出）。Host 代理 `127.0.0.1:7890` 在线，且可从容器内经 `host.docker.internal:7890` TCP 连通。
- 重建：`docker compose -p anti-reverse-demo -f docker-compose.demo.yml build --build-arg HTTP_PROXY=http://host.docker.internal:7890 --build-arg HTTPS_PROXY=http://host.docker.internal:7890 --build-arg NO_PROXY=localhost,127.0.0.1,.docker.internal app` → 新镜像 `ca744fddc8f7`（Aug 6 23:38），BUILD_EXIT=0（6m48s）。
- `up -d --force-recreate app` 重建容器（demo-seed 重跑成功、app 用新镜像）。验收：容器 healthy、`/api/live` 200、`/devices` 3 台 CT、容器内 `sync-iot`/脚本/`devices.json` 均在 → 确认跑的是 23addb0 最新代码。
- 分支领先 origin/codex/docker-demo-deployment 3 个 commit（合并未推）；用户本次未要求推送。
- 经验：daemon 无出网时，buildx 缓存（~16GB）在可纯离线构建；缓存未命中则传 host 代理 build-arg 联网拉包（Dockerfile 已预留 `ARG HTTP_PROXY`）。
