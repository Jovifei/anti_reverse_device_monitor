# 2026-08-04 工作日志

## Docker 部署诊断（anti_reverse_device_monitor_delivery_v1）
- 代码已就绪：tsc 0 错、next build 可编译。已修复 fleetListHref(typedRoutes)、@types/react-dom、metric-match reportedAt、端口 3000→3102、rebuild-offline-review-from-static 缺 inverterGenerationLabel、faults 缺 eventType；确保 prisma generate 先于 build。
- Dockerfile 构建阶段注入代理 ARG/ENV（仅构建期，不影响运行时容器）。
- 阻塞点（环境/网络，非代码）：Docker 守护进程拉镜像无可用代理。`daemon.json` 的 `proxies` 仅作用于容器注入，不作用于守护进程拉取；宿主机真实代理 127.0.0.1:7890 当前未运行且被禁用；无直连出口；无缓存 node 基础镜像。
- daemon.json 代理已从失效的 :3128 更正为 :7890（host.docker.internal）。
- 修复需用户：启动代理(7890, 开启 Allow LAN) + Docker Desktop Settings→Resources→Proxies 设为 http://host.docker.internal:7890 + Apply&Restart。之后由我执行 compose build/up/seed 并验证 http://127.0.0.1:3102/devices。
- 详细报告：DOCKER_DEPLOY_BLOCKER.md。本地修复文件均未 git 提交。
