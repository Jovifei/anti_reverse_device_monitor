# 2026-08-07 工作日志

## 本机 Mongo 配置 + 拉取真实后台数据

- 用户给 Mongo 凭据：`mongodb://zeicomongo@proxy.zeico.cn:3718/?authSource=zeico_cloud`（密码见本机 `.env.local`，gitignored，未入库，不在此记录明文）。
- **`.env.local` 其实早就在本机配好了**（含 URI/MONGODB_DATABASE=zeico_cloud/MONGODB_COLLECTION=device_log_689adc659f04ec32f7642fbb/SOURCE_DB_ENABLED=true），被 gitignore 保护，不会推远端。本次只补了缺失的 `APP_DATABASE_URL` 和 `SOURCE_DB_TYPE=mongodb`（否则 source:sync 报 "unset" / APP_DATABASE_URL required）。
- 只读探针验证：账号能读指定集合 `zeico_cloud.device_log_689adc659f04ec32f7642fbb`，COUNT≥100000；文档结构 `_id/time(epoch)/data{SIID_PIID}/device_id/expireTime`。
- 本地 SQLite 建库：`node scripts/ensure-db-migrations.mjs`（需内联 export APP_DATABASE_URL，路径 `file:../data/device-monitor.db` 即 `E:/project/data/`），0001–0005 全 applied。
- dry-run 单设备验证通过（1252 行、0 失败、device_id→SN 映射正常）。
- 全量 `source:sync` 后台跑（task GTbarO，12 台注册设备 × 7 天回溯），代理偶发连接超时，重跑即恢复；进度健康，已写 26k+ 行、0 错误。

## 关键发现：注册表 vs 真实机群严重不匹配
- 真实集合 `distinct device_id` = **370** 台；`config/devices.json` 仅 **12** 台。
- 当前 sync 只拉注册表里的 12 台，**覆盖约 3%**。要拉全量需先补全注册表。

## "从服务器获取 SN/device id" 的 GitHub 方法
- 即 `scripts/sync-iot-device-registry.ts`（`npm run devices:sync-iot`，提交 `7e809b3`「集成造梦者 IoT 平台自动同步设备注册表」）。
- 调 造梦者 IoT `POST /api/device/getDevices`，拉 device_id↔SN↔nickname 写回 config/devices.json。
- **前置条件**：`.env.local` 里需 `DREAM_MAKER_IOT_TOKEN`（Bearer），当前未配置。用户只给了 Mongo 密码，未给 IoT Token。
- 待用户确认：提供 IoT Token 刷新到全量 370 台注册表，还是先用现有 12 台。

## 注意
- 代理 `proxy.zeico.cn:3718` 偶发 connect timeout（~15s），重试可恢复；非凭据问题。
- `inspect:mongodb` 脚本有 bug：listCollections 失败(error 13)后会抛未捕获 MongoServerError，且不重写报告（报告停在 8/4 旧文件）。实际只读访问正常，别被该脚本误导。

## 续：IoT Token 已给 + 拉取 12 台验证（devices:sync-iot）

- 用户给了 造梦者 IoT Bearer Token（JWT，exp 2026-08-12）和真实端点 `http://iot.iald.cn/api/device/getDevices`，已写入 `.env.local`（`DREAM_MAKER_IOT_TOKEN` / `DREAM_MAKER_IOT_BASE_URL=http://iot.iald.cn`，gitignored，未推远端）。用户意图：先跑这 12 台，后面扩展。
- **发现并修复 schema bug**：真实接口返回 Spring Data 分页（`data.content` / `data.totalElements` / `data.totalPages`），旧代码只认 `data.list` / `data.total`，导致 `safeParse` 通过但取不到列表、首页静默返回空 0 台。
  - 改 `src/adapters/iot-api/types.ts`：`iotListResponseSchema` 增加 content/totalElements/... 字段 + 辅助函数 `iotListContent()` / `iotListTotal()`。
  - 改 `src/adapters/iot-api/iot-client.ts`：`listAllDevices` 改用 `iotListTotal(response.data)` / `iotListContent(response.data)`。dry-run 跑通验证（6a02f379 现在能拉到真实 9 台，修复前为 0）。
- **关键结论：IoT Token 拉不到这 12 台 CT 的映射**。
  - productId `689adc659f04ec32f7642fbb`（CT 品类，与 Mongo 集合同名）→ IoT 返回 **0** 台。
  - productId `6a02f379ad14f363eef24890`（用户示例里带的）→ IoT 返回 **9** 台：8 台非 CT（`3cdc75...` / `a4cb8fc4be58`）+ 仅 **1** 台 CT（`GC2001000000038` / `6969cbb8205d9219dcefda3f`）。
  - 其余 11 台 CT 在该 Token 账号下完全不可达。所以 12 台 CT 的 device_id↔SN 只能来自既有的手动 `config/devices.json`（已正确驱动 Mongo sync）。

## 提交记录（2026-08-07）
- 提交 `5a997c4`（分支 `家里docker`，非 main）：IoT schema 兼容性修复（types.ts + iot-client.ts，2 文件 / +37 -4）。
- **未提交** 本工作日志（内部 scratch）；**未推送远端**（遵守"不推到远端"）。
- 已将上述日志第 5 行明文 Mongo 密码抹除，避免口令进入 git 历史。
- 提交 `178df75`（同分支）：`.gitignore` 增加 `source-sync.log` 并 `git rm --cached` 停止跟踪（保留本地文件，后台同步不受影响）。
- **source:sync 范围确认正确**：`mongo-log-source-adapter.fetchTelemetry` 走 `loadDeviceRegistry().filterEntries()`，只拉注册表里的 12 台，不是全量 370。GTbarO 后台已写 63k+ 行（仍跑），全部为这 12 台 CT 的遥测。
- **待决策（问用户）**：(a) 维持手动 12 台注册表（推荐，已工作）；(b) 用户提供真正列出全部 12 台 GC2001 的 CT productId 再用 IoT 刷新；(c) 扩展到 Mongo 里全部 ~370 台 device_id（但缺 SN/label，IoT 也映射不了）；(d) 顺带把 6a02f379 的 8 台非 CT 也注册进去（可能无 Mongo 数据，徒增噪声）。
